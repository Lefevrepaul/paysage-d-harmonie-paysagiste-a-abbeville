/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // Template uses local assets only for demo; keep remotePatterns empty.
    // This also avoids common misconfigurations that can affect security.
  },
};

export default nextConfig;

